# Discord webhook for CVE notifications
# Create a webhook in Discord and paste the URL here
DISCORD_WEBHOOK_CVES=https://discord.com/api/webhooks/XXXX/YYYY

# Optional: increase NVD rate limits if you have an API key
# NVD_API_KEY=

# Optional defaults (can be overridden by flags)
# CVEWATCH_MIN=8.0
# CVEWATCH_STATE_TTL_HOURS=2
